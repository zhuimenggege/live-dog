package consts

const (
	ListF   = `{#list-failed}`
	AddF    = `{#add-failed}`
	UpdateF = `{#update-failed}`
	DeleteF = `{#delete-failed}`
	GetF    = `{#get-failed}`

	IDEmpty       = `{#id-empty}`
	DataCodeEmpty = `{#data-code-empty}`
	DataNotFound  = `{#data-not-found}`

	ErrLoginFailMsg     = `{#err-login-fail-msg}`
	ErrLoginCodeFailMsg = `{#err-login-code-fail-msg}`
	ErrAuthFailMsg      = `{#err-auth-fail-msg}`
	Success             = `{#success}`
)
