package consts

const (
	MonitorTimeEmpty  = `{#monitor-time-empty}`
	MonitorTimeSame   = `{#monitor-time-same}`
	MonitorTimeShort  = `{#monitor-time-short}`
	RoomUrlEmpty      = `{#room-url-empty}`
	RoomUrlParseF     = `{#room-url-parse-failed}`
	RoomUrlNotAllowed = `{#room-url-not-allowed}`
)
